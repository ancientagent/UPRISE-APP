# Ignore the local git repository to protect the project's version history.
.git/

# Explicitly ignore ONLY the main .env file that contains secrets.
# This allows .env.example, .env.backup, and .env.dev to be visible in Cursor.
.env

# Explicitly allow environment template and backup files
!.env.example
!.env.backup
!.env.dev
