# Ignore the local git repository to protect the project's version history.
.git/

# Ignore ONLY the root .env file, which contains secrets.
# The leading slash anchors this to the root directory and ensures
# it does not incorrectly ignore .env.example or .env.dev.
/.env